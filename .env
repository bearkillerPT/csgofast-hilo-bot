# Environment configuration for csgofast
# Lines starting with # are comments and ignored by dotenv loaders.

# Note: these variables are read by `config.EnvConfig` and used to create the
# selected strategy object. `BASE_BET` acts as the default/minimum bet passed
# into strategy constructors (used as default_bet_amount in the bot).

# Select strategy: 'paroli' (default), 'martingale', or 'fractional'
BET_STRATEGY=paroli

# Base bet used by strategies (integer or float). This is the default bet the
# bot will start with and is passed into strategy constructors as a fallback.
BASE_BET=25

# Paroli-specific parameters
# multiply stake after each win while under target streak
PAROLI_MULTIPLIER=2
# number of consecutive wins to aim for before resetting (bank)
PAROLI_TARGET_STREAK=3

# Martingale-specific parameters
# multiply stake after each loss (commonly 2 to double)
MARTINGALE_MULTIPLIER=2

# Fractional strategy parameters (defaults chosen to match FractionalStrategy)
# minimum bet the fractional strategy will attempt to place (falls back to BASE_BET)
FRACTIONAL_MIN_BET=25
# optional absolute cap on bet size (uncomment to enable)
#FRACTIONAL_MAX_BET=
# thresholds (balance bands) used by the fractional strategy
FRACTIONAL_SMALL_THRESHOLD=500
FRACTIONAL_MEDIUM_THRESHOLD=5000
# fractions used for each band (1.0 means 100% of balance)
FRACTIONAL_SMALL_FRACTION=1.0
FRACTIONAL_MEDIUM_FRACTION=0.5
FRACTIONAL_MEDIUM_MAX_FRACTION=0.75
FRACTIONAL_HIGH_FRACTION=0.25

# Logging
LOG_DIR=logs               # folder where logs will be written (defaults to ./logs)
